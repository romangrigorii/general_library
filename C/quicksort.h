#ifndef quicksort
#define quicksort

#include <stdio.h>

// given an array of integer values 'arr', and array size 'l', returns the array sorted by increasing value
int * quicksort_i(int * arr, int l);
// given an array of double values 'arr', and array size 'l', returns the array sorted by increasing value
double * quicksort_d(double * arr, int l);

#endif
